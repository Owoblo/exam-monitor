services:
  - type: web
    name: exam-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 12 --timeout 120 server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0

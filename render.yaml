services:
  - type: web
    name: exam-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gevent --workers 2 --timeout 300 server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
